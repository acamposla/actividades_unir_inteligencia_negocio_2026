knitr::opts_chunk$set(echo = TRUE)
library(readxl)
library(caret)
library(dplyr)
library(readxl)
library(caret)
library(dplyr)
library(readxl)
library(caret)
library(dplyr)
getwd()
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
View(diabetes)
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
# cuántas filas y columnas tienes
str(diabetes)
# cuántas filas y columnas tienes
dim(diabetes)
# Qué tipo de dato tiene cada variable
str(diabetes)
# cuántas filas y columnas tienes
print("el número de filas es " nrows(diabetes))
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
# cuántas filas y columnas tienes
print(f"el número de filas es  nrows(diabetes)")
library(readxl)
library(caret)
library(dplyr)
library(glue)
getwd()
# cuántas filas y columnas tienes
glue(f"el número de filas es  nrows(diabetes)")
library(readxl)
library(caret)
library(dplyr)
library(glue) # para hacer fstring
getwd()
# cuántas filas y columnas tienes
glue("el número de filas es {nrows(diabetes)}")
# cuántas filas y columnas tienes
glue("el número de filas es {nrow(diabetes)}")
glue("el número de columnas es {ncol(diabetes)}")
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
# cuántas filas y columnas tienes
glue("el número de filas es {nrow(diabetes)}")
glue("el número de columnas es {ncol(diabetes)}")
# Qué tipo de dato tiene cada variable
```{r Pregunta 1}
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
# cuántas filas y columnas tienes
glue("el número de filas es {nrow(diabetes)}")
glue("el número de columnas es {ncol(diabetes)}")
# Qué tipo de dato tiene cada variable
# Qué tipo de dato tiene cada variable
glue({str(diabetes)})
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
# cuántas filas y columnas tienes
glue("el número de filas es {nrow(diabetes)}")
glue("el número de columnas es {ncol(diabetes)}")
# Qué tipo de dato tiene cada variable
glue({str(diabetes)})
# Qué tipo de dato tiene cada variable
glue("Aqui se pueden ver el tipo de variables {str(diabetes)}")
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
# cuántas filas y columnas tienes
glue("el número de filas es {nrow(diabetes)}")
glue("el número de columnas es {ncol(diabetes)}")
# Qué tipo de dato tiene cada variable
glue("Aqui se pueden ver el tipo de variables {str(diabetes)}")
diabetes <- read.csv("/Users/alejandrocamposlamas/Proyectos/ACTIVIDADES_UNIR/Estudiar_R/datasets/diabetes.csv")
# cuántas filas y columnas tienes
glue("el número de filas es {nrow(diabetes)}")
glue("el número de columnas es {ncol(diabetes)}")
# Qué tipo de dato tiene cada variable
glue("Aqui se pueden ver el tipo de variables {str(diabetes)}")
sum(is.na(diabetes))
glue("Existe {sum(is.na(diabetes)} nulos en el dataset)
glue("Existe {sum(is.na(diabetes)} nulos en el dataset")
sum(is.na(diabetes)
sum(is.na(diabetes))
glue("Existe sum(is.na(diabetes)) nulos en el dataset")
glue("Existe {sum(is.na(diabetes))} nulos en el dataset")
unique(diabetes$Glucose)
# filtramos por los registros que tienen 0 en estas variables
datos %>% filter(type == "organic")
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetesZ %>% filter(BloodPressure = 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(BloodPressure = 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(BloodPressure == 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
View(diabetes)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
colSums(diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0))
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 1
colSums(diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0))
# intento 2
colSums(is.zero(diabetes))
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 1
colSums(diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0))
# intento 2
# colSums(is.zero(diabetes))
# intento 3
colSums(diabetes == 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
colSums(diabetes == 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
colSums(diabetes[ ,Glucose]== 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
colSums(diabetes[ ,"Glucose"]== 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
colSums(diabetes[ ,'Glucose']== 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
colSums(diabetes[ ,"Glucose"]== 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
columnas_sospechosas <- c("Glucose")
colSums(diabetes[ ,columnas_sospechosas] == 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
cols_sospechosas <- c("Glucose", "BloodPressure", "SkinThickness", "Insulin", "BMI")
colSums(diabetes[, cols_sospechosas] == 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
cols_sospechosas <- c("Glucose", "BloodPressure", "SkinThickness", "Insulin", "BMI")
colSums(diabetes[,  c("Glucose")] == 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
cols_sospechosas <- c("Glucose", "BloodPressure", "SkinThickness", "Insulin" , "BMI")
colSums(diabetes[,  ] == 0)
# filtramos por los registros que tienen 0 en estas variables
#datos %>% filter(type == "organic")
diabetes %>% filter(Glucose == 0 | BloodPressure == 0 | SkinThickness == 0 | Insulin == 0 | BMI == 0)
# intento sumar el número de registros con valor 0 en cada una de las variables. Parece que debería utilizar colsums(), pero solo tengo colSums(is.na(datos)) no me valdría, no sabría hacerlo y no lo tengo en la chuleta.
# intento 2
# colSums(is.zero(diabetes))
# intento 3
cols_sospechosas <- c("Glucose", "BloodPressure", "SkinThickness", "Insulin" , "BMI")
colSums(diabetes[, cols_sospechosas ] == 0)
diabetes %>% filter("Glucose" =! 0 | "BMI" =! 0)
diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
nrows(diabetesf)
diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
nrow(diabetesf)
diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
nrow(diabetesf)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter("Glucose" != 0 | "BMI" != 0.0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter("Glucose" != 0 | "BMI" != 0.0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter("Glucose" != 0 | "BMI" > 0.0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter("Glucose" != 0 | "BMI" > 0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter("Glucose" != 0 & "BMI" != 0.0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter("Glucose" != 0 & "BMI" != 0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter(Glucose != 0 & BMI != 0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter(Glucose != 0 | BMI != 0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetes %>% filter(Glucose != 0 & BMI != 0)
#diabetesf <- diabetes %>% filter("Glucose" != 0 | "BMI" != 0)
# nrow(diabetesf)
diabetesf <-diabetes %>% filter(Glucose != 0 & BMI != 0)
nrow(diabetesf)
